#!/usr/bin/env node
/**
 * Placeholder version bump script. Update manifest.json manually for now.
 */
console.log('Update manifest.json and package.json versions manually, then rebuild the plugin.');
